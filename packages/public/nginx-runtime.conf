server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Astro emits directories with index.html — try the path, then path/index.html
    location / {
        try_files $uri $uri/ $uri.html $uri/index.html =404;
    }

    error_page 404 /404.html;
}
